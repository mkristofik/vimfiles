setlocal expandtab
setlocal shiftwidth=4
setlocal tabstop=4

" Wrap long lines when they get close to the edge.
setlocal textwidth=0
setlocal wrapmargin=2

setlocal autoindent

" auto-wrap text but don't insert new bullets
setlocal formatoptions=t
